# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Meta / WhatsApp Cloud API ---
META_APP_ID=
META_APP_SECRET=
META_SYSTEM_USER_TOKEN=
META_GRAPH_VERSION=v22.0
WEBHOOK_VERIFY_TOKEN=

# --- Web Push (VAPID) ---
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:victor@7bee.ai
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# --- Internal ---
CRON_SECRET=
# Public origin used by SQL trigger (pg_net) to call /api/internal/push/notify
APP_ORIGIN=http://localhost:3000

# --- Runtime ---
# Porta local do Next. Default 3000. Sobrescreva se a VPS tiver outro app
# na 3000 (caso da nossa: waytop-portal ocupa, chat-cdt fica na 3007).
# PM2, nginx e health check do deploy.sh leem este valor.
PORT=3000
