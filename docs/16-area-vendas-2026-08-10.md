# 16. Área de Vendas — monitor ao vivo da Josi (2026-08-10)

## O que é

Nova área `/vendas` no CHAT-CDT: um **monitor em tempo real, somente leitura**,
de TODAS as conversas dos números de VENDAS (agente Josi). É o oposto da inbox:
lá só aparecem handoffs (`routing≠'ai'`); aqui o caso principal é exatamente a
IA conduzindo (`routing='ai'`).

## Por que as conversas de vendas não apareciam em lugar nenhum

Duas causas, ambas silenciosas:

1. **Webhook do chat descartava o inbound.** Nenhum dos 6 números de vendas
   ativos (SPB/VIT/PAT/CGT/MGC/TT) estava cadastrado em `chat_phone_numbers` →
   `unknown phone_number_id` → `continue` (mesmo gate da lição patrocinio001,
   doc de 2026-06-15). O audit `chat_webhook_events` provava que os eventos
   CHEGAVAM (Caraguá: 118 em 14 dias).
2. **Outbound da Josi falhava em silêncio.** O workflow de vendas do n8n tem o
   nó `registrar_outbound_chat` → `chat_record_outbound_message(...)`, que
   LANÇA EXCEÇÃO quando o phone_number_id não está cadastrado. O nó tem
   `onError: continueRegularOutput`, então cada resposta da Josi era enviada ao
   cliente mas o log no chat morria calado.

## O que foi feito

### Dados (aplicado direto em prod, 2026-08-10)

- 2 linhas novas em `wabas`: WABAs de vendas de Tatuapé (`4411556725747181`) e
  Mogi (`1590169158752806`) — WABAs novas da troca de 05/08 que só carregam o
  número de vendas (os números de COBRANÇA de TT/MGC seguem nas WABAs antigas,
  já cadastradas — verificado no audit, recebendo normalmente).
- 6 linhas novas em `chat_phone_numbers` (números de vendas por unidade).
- A partir daí o pipeline padrão passa a funcionar sozinho: inbound cria
  contact/conversation/message; `chat_record_outbound_message` loga a Josi
  (e cria a conversa se o primeiro evento for outbound).

### Migration 0026 (`infra/supabase/migrations/0026_area_vendas.sql`)

- `chat_vendas_phone_rows()` — classificador: linhas de `chat_phone_numbers`
  que são números de vendas (join com `unit_sales_config` do n8n), com
  `vendas_ativo`. O app filtra `ativo=true`, o que hoje exclui **Formiga**
  (número compartilhado com cobrança/relacionamento, vendas desligada).
- `chat_vendas_lead_estados(uuid[])` — estado do funil (`vendas_leads`) por
  conversa, casado por unidade + `chat_phone_match_key`.
- Ambas SECURITY DEFINER gated por `chat_user_has_unit`, somente leitura.

### App

- `app/(app)/vendas/` — layout (working set = conversas com
  `phone_number_id ∈ chat_vendas_phone_rows()`, previews e estados em lote),
  lista com abas **IA ao vivo / Com humano / Encerradas / Todas**, busca e
  filtro de unidade (o mesmo UnitSelect global).
- Realtime nas 3 camadas da inbox: canal `vendas-workspace`
  (conversations + messages), stub + `router.refresh()` debounced para
  conversa nova, polling de fallback 60s.
- `app/(app)/vendas/[id]` — thread em **modo monitor**: reusa
  `ThreadPane/ThreadClient/ThreadHeader` com prop `monitor` (sem composer,
  sem Assumir/Devolver/Encerrar; chip "IA ao vivo"/"Com humano"/"Encerrada";
  rodapé explica o modo). Realtime da thread é o canal por conversa que já
  existia.
- Sidebar ganhou o item **Vendas** (ícone Sparkles), visível para todos —
  a RLS por unidade continua decidindo o que cada um vê.

## Por que somente leitura (decisão consciente)

O workflow de vendas foi clonado do receptivo de cobrança **sem** o gate
`ai_may_send` (poda registrada em 2026-07-28). Ou seja: se um operador
respondesse por aqui, **a Josi continuaria respondendo em paralelo** — duas
vozes na mesma conversa. Até o gate existir nos 8 workflows de vendas,
intervenção humana é pela Inbox (quando a conversa vira handoff) e o monitor
não envia nada.

## Efeitos colaterais esperados (e desejados)

- `ai_no_response` (edge function agent-tools) agora ENCONTRA a conversa de
  vendas e a enfileira como handoff `other_support` → conversas de vendas em
  falha passam a aparecer na **Inbox** normal (antes: no-op silencioso, lead
  no vácuo). Em Patrocínio, o rodízio (migration 0022) atribui direto.
- Essas conversas também aparecem na Área de Vendas com badge "Com humano".

## Fase 1 do onboarding/Boas Vindas (mesma data, mais tarde)

Pedido de Caraguá (Sérgio/Vagner, grupo, 22-23/07): o setor de **Boas Vindas**
(que envia o link de validação facial/KYC manual pós-venda) atendendo os novos
filiados **dentro do CHAT-CDT**. Implementado:

- **Gate `ai_may_send` nos 7 workflows CDT Vendas** (padrão copiado do CDT
  Cobrança: `Tem mensagem? → gate_ai_may_send → IF`). Com conversa
  `queued`/`human`, a Josi silencia — resolvida a pendência de "duas vozes".
- **Motivo de handoff novo `boas_vindas`**: enum `chat_handoff_reason`,
  `TRANSFER_REASONS` do agent-tools (v13), labels/tons/ícone no app
  (HeartHandshake, esmeralda) e cor no relatório. Filtro de motivo da Inbox
  ganhou "Boas-vindas" automaticamente.
- **Auto-handoff na conversão**: `vendas_registrar_estado` (migration
  `20260810210000` no repo cpt-ibirite) — em `CONCLUIDO_DECLARADO` de unidade
  com `unit_sales_config.kyc_manual=true` (hoje só Caraguá), a conversa aberta
  em `routing='ai'` vira `queued`/`boas_vindas` na hora. A resposta do MESMO
  turno da Josi ainda sai (gate roda no início da execução); da mensagem
  seguinte em diante o humano conduz. Falha no handoff não derruba a tool.
- A tool `agent_transfer_human` dos 7 workflows ganhou orientação de quando
  usar `reason='boas_vindas'` (o prompt da Josi Caraguá já mandava transferir
  na confirmação da validação facial).

Testado ponta-a-ponta com conversa sintética em Caraguá (criada, promovida a
`queued/boas_vindas` pela RPC, deletada).

## Pendências / próximos passos

- **Rodízio (auto-assign) para Caraguá**: o mecanismo da migration 0022 é
  por unidade e **pega TODOS os handoffs da unidade**, não só boas-vindas —
  ligar só quando definido quem recebe (e se a franquia quer rodízio geral).
- Formiga: se a vendas religar no número compartilhado, conversas de
  cobrança/relacionamento vão se misturar às de vendas na área (o
  classificador é por número). Resolver quando/se acontecer.
- Histórico: eventos antigos descartados pelo webhook NÃO são reprocessados
  (replay manual de `chat_webhook_events` se algum histórico importar).
- Fase 2: follow-up automático de KYC pendente (CONCLUIDO_DECLARADO sem
  CONFIRMADO após X horas) + contador de "aguardando validação" na área.
